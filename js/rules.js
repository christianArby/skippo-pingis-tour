// Rules-vyn är helt statisk — markup ligger i index.html.
// render() finns för att vy-routern i app.js ska kunna anropa
// den uniformt med övriga vyer.

export async function render() {
  // ingen DB-data, ingen rendering behövs
}
